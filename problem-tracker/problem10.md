# 问题 10：Clean-room baseline 命名和语义不严谨

## 评估

部分成立，中优先级。

当前 `_clean_room_baseline()` 直接把 `ground_truth` 作为上下文喂给同一个生成模型和 Judge。它可以测“答案注入后的生成自洽性”，但不是真实文档上下文下的严格生成上界。

## 影响

标准答案可能比原始文档更整理；同一答案同时作为 context 和评测参照，也可能造成措辞偏置。

## 结论

该实验有价值，但应改名并与“原始 gold 文档上下文 baseline”分开。
