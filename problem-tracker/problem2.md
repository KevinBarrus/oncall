# 问题 2：上下文计数没有覆盖 Agent 运行时内容

## 评估

确认成立，高优先级。

`estimate_context_tokens()` 只计算 system prompt、memory summary 和 user/assistant 消息。真实聊天 Agent 还会产生工具调用参数、工具结果、模型输出和消息包装开销。

## 影响

当前 95% 阈值不是对实际请求的严格上限。压缩后仍可能因工具循环或输出预留不足而被模型服务拒绝。

## 结论

工具输出压缩只能缓解问题，不能替代完整的运行时预算管理。
