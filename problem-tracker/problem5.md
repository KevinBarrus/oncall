# 问题 5：同一会话缺少并发控制

## 评估

确认成立，高优先级。

当前流程先由 `prepare_message()` 读取并更新记忆状态，再由聊天流追加用户消息；没有看到按 session_id 的锁、版本号 CAS 或串行队列。

## 影响

两个并发请求可能基于相同的 `compacted_message_count` 压缩同一批历史，互相覆盖摘要和 token 状态，并导致消息顺序与摘要边界不一致。

## 结论

这是会话一致性问题。单进程本地部署可先用锁降低风险，但持久化 CAS 才是跨进程正确性基础。
