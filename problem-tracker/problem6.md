# 问题 6：评测没有覆盖真实聊天系统

## 评估

确认成立，高优先级。

`tests/ragas_evaluation.py` 的主流程直接执行检索和生成函数，没有经过真实 `/chat` Agent、工具注册、工具输出压缩、记忆管理、SSE、MCP、权限和会话上下文。

## 影响

评测结果只能说明独立 RAG prompt 的效果，不能证明真实聊天系统在工具调用、上下文和权限边界下表现良好。

## 结论

离线检索评测仍有价值，但必须明确命名为 pipeline evaluation，不能作为端到端聊天质量结论。
