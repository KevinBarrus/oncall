# 问题 8：确定性指标依赖词面重叠

## 评估

确认成立，中高优先级。

当前 `_token_overlap()` 使用交集除以较短文本 token 数，并据此自动生成相关性标签，再计算 MRR、NDCG 和 Recall。这样会让共享少量关键词但没有回答问题的 chunk 获得高分。

## 影响

指标与标签存在循环依赖；Recall@5 更接近答案词覆盖，不等于事实或要点覆盖；Markdown 表格、同义表达和数字关系容易被误判。

## 结论

当前指标可以作为快速诊断信号，不能作为高可信检索质量标签。
