# 问题 9：LLM Judge 失败被伪装为 0.5 分

## 评估

确认成立，高优先级。

`_parse_score()` 无法解析时返回 0.5，`_llm_score()` 在异常时也返回 0.5。评测失败、格式错误和真实中等得分因此无法区分。

## 影响

Judge 服务不可用时仍会生成看似正常的平均分，污染策略比较和回归结论。

## 结论

这是评测系统的正确性问题，优先级高于继续扩充指标数量。
