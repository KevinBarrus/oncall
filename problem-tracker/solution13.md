# 问题 13：解决方案

## 计划

1. 为反馈定义幂等键，至少包含 task_id、user_id 和反馈类型；若业务允许修改反馈，再加入版本或采用 upsert。
2. 增加数据库唯一约束和 Alembic 迁移。
3. Repository 使用事务化 upsert，重复请求返回已有反馈结果。
4. 审计记录区分首次提交、重复提交和修改提交。
5. 增加请求重试、重复点击和并发提交测试。

## 验收标准

- 同一反馈重放不会重复增加 Bayesian 权重。
- 并发提交最终只有一个有效反馈版本。
- 反馈修改和撤销都有清晰审计记录。
