# 问题 6：解决方案

## 计划

1. 保留现有离线检索评测，定位召回、排序和生成基础能力。
2. 增加端到端聊天评测入口，复用真实 Agent runner 或真实 API/SSE 链路。
3. 覆盖记忆压缩、工具调用、工具失败、权限拒绝、引用和 SSE 终止事件。
4. 测试中使用可控的 fake MCP/LLM，但保持真实运行时编排不变。
5. 分开展示 pipeline、runtime 和 end-to-end 指标。

## 验收标准

- 报告明确区分三类评测。
- 至少有一组测试经过真实聊天运行时。
- 工具压缩、记忆和权限错误能在端到端结果中被观测。
