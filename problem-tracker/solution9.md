# 问题 9：解决方案

## 计划

1. 将 Judge 结果改为 `score: float | None`。
2. 失败时返回 `judgeError`、错误类型和原始响应摘要。
3. 聚合时排除 null，但单独报告 judge failure rate。
4. 使用严格格式解析，拒绝从任意文本中取第一个数字。
5. 设置最低有效样本数，不足时标记评测结果不可比较。

## 验收标准

- 真实 0.5 与 Judge 失败可区分。
- 失败率单独统计，不进入正式平均分。
- 格式错误不会被静默转换成合法分数。
